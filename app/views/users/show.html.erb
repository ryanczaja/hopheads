<div class="row">
	<div class="span12">
		<div class="media">
			<div class="media-body">
				<h1 class="media-heading"><%= @user.name %></h1>
				<small>
					<%= pluralize(@user.posts.count, 'Bottle Caps') %> |
					<%= pluralize(@user.comments.count, 'comment') %>
				</small>
			</div>
		</div>
	</div>
</div>

<div class="row">
	<br />
	<!-- Renders the users posts. -->
	
	<%= render @posts %>
</div>