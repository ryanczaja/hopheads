<div class="row">
	<div class="span8">
		<h1> <%= @post.title %> </h1>

		<b>Beer Name:</b> <%= @post.beer.name %><br />
		<b>Beer Style:</b> <%= @post.beer.style %><br />
		<b>Alcohol Percentage:</b> <%= @post.beer.abv %>%<br />
		<b>Beer Description:</b> <%= @post.beer.description %><br />

		<hr />
		<h2><%= @comments.count %> Comments </h2>

		<%= render @comments %>

		<% if can? :create, Comment %>
			<hr />
			<h4>New Comment</h4>
			<%= render partial: 'comments/form', locals: {post: @post, comment: @comment} %>
		<% end %>
	</div>

	<div class="span4">
		<% if can? :edit, @post %>
			<%= link_to "Edit", edit_post_path(@post), class: 'btn btn-large btn-block' %>
		<% end %>

		<% if can? :destroy, @post %>
			<%= link_to "Delete Post", @post, method: :delete, class: 'btn btn-large btn-block btn-danger', confirm: 'Are you sure you want to delete this Post?' %>
		<% end %>
	</div>
</div>

