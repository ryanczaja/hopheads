<div class="row-fluid">
	<div class="span6">
		<h2>Recently Drank Beers</h2>
	</div>

	<div class="span6">
		<div class="span4 pull-right">
			<% if can? :create, Post %>
				<%= link_to "New Post", new_post_path, class: 'btn btn-large btn-block' %>
			<% end %>
		</div>
	</div>
</div>

<div class="row">
	<!-- This goes into the post partial in the posts folder -->
	<%= render @posts %>
</div>